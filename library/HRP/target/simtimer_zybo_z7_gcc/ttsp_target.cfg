/*
 *  タイマドライバシミュレータ(simt)版ターゲット依存のTTSP設定
 *
 *  【改変】時刻を任意量進める拡張SVC ttsp_simt_advance(N) を定義する．
 *  本ファイルはターゲット依存（simt のみ）で，非simtターゲットの ttsp_target.cfg
 *  は空のため，他ターゲットの DEF_SVC には影響しない．
 */
#include "ttsp_target_test.h"

KERNEL_DOMAIN {
	DEF_SVC(TTSP_FN_SIMT_ADVANCE, {TA_NULL, svc_ttsp_simt_advance, TTSP_SVC_STACK_SIZE});
}
