version: "$Id: dis_dsp_c_H_ex.yaml 75 2020-03-19 08:28:47Z fujisft-shigihara $"
ASP_sys_manage_dis_dsp_c_H3:
  note: 【NGKI2745】

  pre_condition:
    TASK1:
      type   : TASK
      tskstat: running
      domain : DOM_SYS_ACP1

    CPU_STATE:
      type   : CPU_STATE
      dis_dsp: true
      access1: TACP(DOM_SYS_ACP1)
      access2: TACP_KERNEL
      access3: TACP_KERNEL
      access4: TACP_KERNEL

    DOM_SYS_ACP1:
      type: DOMAIN

  do:
    id     : TASK1
    syscall: dis_dsp()
    ercd   : E_OK

  post_condition:

ASP_sys_manage_dis_dsp_c_H5:
  note: 【NGKI2745】

  pre_condition:
    TASK1:
      type   : TASK
      tskstat: running
      domain : DOM1

    CPU_STATE:
      type   : CPU_STATE
      dis_dsp: true

    DOM1:
      type: DOMAIN

  do:
    id     : TASK1
    syscall: TTSP_DIS_DSP()
    ercd   : E_OK

  post_condition:
