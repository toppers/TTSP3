version: "$Id: try_spn_F-a.yaml 67 2020-02-04 09:15:32Z fujisft-shigihara $"
FMP_spin_lock_try_spn_F_a:
  note: 【NGKI2188】

  pre_condition:
    TASK1:
      type   : TASK
      tskstat: running
      prcid  : PRC_SELF

    SPN1:
      type   : SPINLOCK
      spnstat: TSPN_UNL

    CPU_STATE1:
      type   : CPU_STATE
      loc_cpu: true
      prcid  : PRC_SELF

  do:
    id     : TASK1
    syscall: try_spn(SPN1)
    ercd   : E_CTX

  post_condition:
