version: "$Id: sig_sem_a.yaml 64 2019-12-20 01:33:52Z fujisft-shigihara $"
ASP_semaphore_sig_sem_a:
  note: 【NGKI1501】

  pre_condition:
    TASK1:
      type   : TASK
      tskstat: running

    SEM1:
      type: SEMAPHORE

    CPU_STATE:
      type   : CPU_STATE
      loc_cpu: true

  do:
    id     : TASK1
    syscall: sig_sem(SEM1)
    ercd   : E_CTX

  post_condition:
