version: "$Id: set_flg_a.yaml 64 2019-12-20 01:33:52Z fujisft-shigihara $"
ASP_eventflag_set_flg_a:
  note: 【NGKI1604】

  pre_condition:
    TASK1:
      type   : TASK
      tskstat: running

    FLG1:
      type  : EVENTFLAG
      flgptn: BIT_PATTERN_0A

    CPU_STATE:
      type   : CPU_STATE
      loc_cpu: true

  do:
    id     : TASK1
    syscall: set_flg(FLG1, BIT_PATTERN_0B)
    ercd   : E_CTX

  post_condition:
