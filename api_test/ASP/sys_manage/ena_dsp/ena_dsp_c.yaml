version: "$Id: ena_dsp_c.yaml 64 2019-12-20 01:33:52Z fujisft-shigihara $"
ASP_sys_manage_ena_dsp_c:
  note: 【NGKI2751】

  pre_condition:
    TASK1:
      type   : TASK
      tskstat: running

    CPU_STATE:
      type   : CPU_STATE
      dis_dsp: false

  do:
    id     : TASK1
    syscall: ena_dsp()
    ercd   : E_OK

  post_condition:
