version: "$Id: rot_rdq_a.yaml 64 2019-12-20 01:33:52Z fujisft-shigihara $"
ASP_sys_manage_rot_rdq_a:
  note: 【NGKI2684】

  pre_condition:
    TASK1:
      type   : TASK
      tskstat: running

    CPU_STATE:
      type   : CPU_STATE
      loc_cpu: true

  do:
    id     : TASK1
    syscall: rot_rdq(TSK_PRI_MID)
    ercd   : E_CTX

  post_condition:
